node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts

.env
.env*.local

/orders/
/outbox/

# Uploaded product photography. Content, not source — it belongs to whatever
# storage IN-01 settles on, not to the repository. See src/lib/storage.ts.
/public/uploads/

.DS_Store
*.pem
npm-debug.log*

# Local launcher — hardcodes this machine's paths and conversation id.
/click here to start.cmd

/src/generated/prisma

# Local development database — recreate with: npx prisma migrate dev
*.db
*.db-journal

# Prisma 7 scaffolds AI-agent skill docs on init. Not part of the app.
.agents/
.windsurf/
.claude/skills/prisma-*/
skills-lock.json

# Private document store (TRN certificates and the like). Never public,
# never committed — see src/lib/storage.ts.
/storage/
